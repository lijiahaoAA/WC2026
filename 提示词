你好！我需要你帮我开发一个“2026世界杯赛事智能大屏指挥中心”的前端页面。我已经设计好了效果图，整体风格是现代、硬朗、科技感十足的深色系。

请根据以下详细的架构和样式要求，使用 Vue 3 (Composition API) + Tailwind CSS（或其他你熟悉的 CSS 框架）来编写代码。

1. 整体视觉与布局 (Layout & Theme)
配色方案： 以极客黑/深蓝（如 #0d1117 或 #050914）为底色。

核心色调： 采用科技蓝（荧光蓝/霓虹蓝）作为主色调和发光特效（Text Glow/Box Shadow），搭配荧光绿作为高亮或状态提示色（如 NEXT MATCH 标签），重要对比信息使用暖橙色/金色。

空间布局： 采用大屏可视化（Dashboard）的对称式卡片布局。整体分为顶栏、左/中/右三栏主体、以及底部滚动赛程。

2. 顶栏 (Header)
左侧： 页面大标题“2026 世界杯赛事智能大屏指挥中心”，文字使用科技蓝并带微弱发光特效。

右侧： 包含三个数据看板组件：

赛事倒计时（如：389天，亮绿色）

参赛队伍（如：48支）

总出场场次（如：104场）

3. 主体左侧：主队信息面板 (Home Team Card)
样式： 采用半透明的毛玻璃效果卡片 (Glassmorphism) 边框带淡淡的蓝色呼吸灯特效。

内容： 展示墨西哥队的详细概况，包含：

球队 Logo 占位区域

核心球员列表（List 结构）

近期战绩（用不同颜色的徽章展示“胜/平/负”）

战术风格标签（如：积极压迫、高位逼抢）

4. 主体右侧：客队信息面板 (Away Team Card)
与左侧面板严格保持视觉对称。

展示南非队的详细概况（核心球员、战绩、战术风格如：快速反击）。

5. 主体中央：核心视觉区 (Center Showcase)
上半部分（对阵信息）： * 巨大的 VS 字样，使用橙色/金色高亮。

主队名称“墨西哥”与客队名称“南非”分列两侧，字体放大并带有强烈的蓝色霓虹发光特效 (text-shadow)。

中间包含赛事详情标签：A组（揭幕战）、比赛时间（2026年06月12日 03:00）、比赛球场。

下半部分（核心 3D 容器）：

预留一个宽大的容器（可以用 div 或 canvas 占位），未来用于承载 Three.js / WebGL 渲染的 3D 体育场模型。

背景带有一层淡淡的全球网络网格（Grid）和几何线条（可以用 CSS Background 实现或 SVG 引入）。

6. 底部：赛程轮播轴 (Bottom Schedule Timeline)
一个横向排列的卡片式赛程表。

单张卡片内容： 包含小组（如 A组、B组）、对阵双方（如 韩国 VS 捷克）、比赛时间、比赛球场。

交互： 整体支持横向滚动（Overflow-X-Scroll），卡片悬停时有放大或边框加亮效果。

请先为我搭出这个页面的整体 HTML 结构与 Tailwind CSS 样式。3D 球场部分先用一个精美的 CSS 渐变或占位图代替，重点是还原这种高科技、大屏指挥中心的视觉冲击力！